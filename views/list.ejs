<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Members List</title>

    <style>
        body {
            overflow-x: auto;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 120vh;
            margin: 0;
        }

        h1 {
            margin-top: 50px;
            margin-bottom: 20px;
            font-size: 36px;
            text-align: center;
        }

        .button {
            padding: 10px;
            border: none;
            border-radius: 5px;
            margin-top: 10px;
            margin-left: 20px;
            background-color: #4CAF50;
            color: white;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .button:hover {
            background-color: #3e8e41;
        }

        .card {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 300px;
            height: 300px;
            border-radius: 5px;
            background-color: #f2f2f2;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            margin-bottom: 20px;
        }

        .container {
            display: flex;
            margin: 10px;
        }

        .box {
            flex: 1;
            height: 100px;
            margin: 10px;
            background-color: #ccc;
        }
    </style>
</head>

<body>

    <h1>Members List</h1>
    <% for (let i=0; i < members.length; i++) { let member=members[i]; %>
        <div class="card">
            <img style="border-radius: 5px; margin-top: 20px;" src="data:image/jpeg;base64, <%= member.photo.toString('base64') %>" alt="photo" width="150px" height="150px">
            <br>
            Name: <%= member.name %>
            <br>
            Email: <%= member.email %>

            <div class="container">
                <a class="button" href="/edit/<%= member._id %>">Edit</a>
                <a class="button" href="/delete/<%= member._id %>">Delete</a>    
            </div>
        </div>
        <br>
    <% } %>

    <a class="button" href="/profile">Return to your profile</a>
</body>

</html>